import React from 'react'

function column() {
    // const item = []
  return (
    <React.Fragment>
        {/* {items.map( item => (
            <React.Fragment key={item.id}>
                <h1>title</h1>
                <h1>{item.title}</h1>
            </React.Fragment>
        ))
        } */}
      <td>Name</td>
      <td>Adarsh</td>
    </React.Fragment>
  )
}

export default column
